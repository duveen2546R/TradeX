# MongoDB Atlas: never commit the real URI or credentials.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/tradex?retryWrites=true&w=majority
MONGODB_DATABASE=tradex
FLASK_SECRET_KEY=replace-with-a-long-random-secret
FRONTEND_ORIGIN=http://localhost:5173
COOKIE_SECURE=false
QUOTE_MAX_AGE_SECONDS=90
KAFKA_BOOTSTRAP=localhost:9092

